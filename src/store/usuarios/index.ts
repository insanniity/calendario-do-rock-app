import {createSlice} from "@reduxjs/toolkit";


type UsuariosState = {

}

const initialState: UsuariosState = {
}

export const usuaruiosSlice = createSlice({
    name: 'usuarios',
    initialState,
    reducers: {
    }
})



export default usuaruiosSlice.reducer;

export const {} = usuaruiosSlice.actions;

